# Address of the deployed BountyEscrow contract (written automatically by
# `npm run deploy` from the project root, or paste it here manually).
VITE_CONTRACT_ADDRESS=

# Which GenLayer network the frontend talks to.
# localnet | studionet | testnetAsimov | testnetBradbury
VITE_GENLAYER_NETWORK=localnet
