# Network to deploy to: localnet | studionet | testnetAsimov | testnetBradbury
GENLAYER_NETWORK=localnet

# Optional. On Studio/Localnet a random pre-funded account is used if this is unset.
# For testnet, set a funded account's private key (fund it at the testnet faucet).
GENLAYER_ACCOUNT_PRIVATE_KEY=
